/** @format */

import React, {Fragment} from 'react';
import Promo from '../Promo';

const Hero = () => {
	return (
		<Fragment>
			<section className='hero'>
				<Promo />
			</section>
		</Fragment>
	);
};

export default Hero;
